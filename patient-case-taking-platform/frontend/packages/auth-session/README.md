# Auth Session

Shared Clerk session primitives, logout, expiry, refresh and step-up behavior with separate app audiences/configuration. Hospital SSO and kiosk pre-auth are exposed through narrow adapters so applications do not depend on identity-provider internals.
