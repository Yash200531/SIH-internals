# Tools

- `local-development`: repeatable local service and dependency startup.
- `fixtures`: synthetic data generation only.
- `migration`: controlled schema, data and integration migration utilities.

Tools must honor the same secret handling, PHI redaction and audit expectations as production services.

