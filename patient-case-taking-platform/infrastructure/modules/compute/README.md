# Compute Module

Next.js applications, FastAPI services, workers, CPU pools and GPU pools with independent scaling and disruption policies. vLLM remains private behind the AI gateway; KServe/Ray Serve are optional later control planes.
