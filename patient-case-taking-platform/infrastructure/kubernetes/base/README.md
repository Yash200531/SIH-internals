# Kubernetes Base

Common Kubernetes manifests for shared pilot and production deployments. Local development remains Docker Compose. Keep stateful services managed where possible, isolate CPU/GPU pools, and do not add a service mesh until measured requirements justify it.
